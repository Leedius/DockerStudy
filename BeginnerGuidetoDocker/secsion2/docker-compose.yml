version: '2'
services:
  db:
    image: mysql:5.7  # 이미지와 태그
    ports:
      - "3307:3307" # 호스트의 3307 -> 컨테이너 내부 3306
    volumes:  # -v 옵션 (데이터 유실 방지)
      - ./mysql:/var/lib/mysql
    restart: always # 항상 재시작
    environment:
      MYSQL_ROOT_PASSWORD: wordpress
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: wordpress
  wordpress:
    image: wordpress:latest
    volumes:
      - ./wp:/var/www/html
    ports:
      - "8000:80"
    restart: always
    environment:
      WORDPRESS_DB_HOST: db:3307
      WORDPRESS_DB_PASSWORD: wordpress